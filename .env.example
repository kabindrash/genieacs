# GenieACS Environment Configuration
# Copy this file to .env and customize for your environment:
#   cp .env.example .env

# --- Required ---

# JWT secret for UI authentication (MUST be changed from default)
# Generate with: openssl rand -hex 32
GENIEACS_UI_JWT_SECRET=changeme-generate-a-random-secret

# --- Optional (defaults shown) ---

# MongoDB
GENIEACS_MONGODB_CONNECTION_URL=mongodb://mongodb/genieacs
MONGO_VERSION=6
MONGODB_PORT=27017

# Service ports (host-side)
CWMP_PORT=7547
NBI_PORT=7557
FS_PORT=7567
UI_PORT=3001

# Access logs (set to a file path to enable, /dev/null to disable)
GENIEACS_CWMP_ACCESS_LOG_FILE=/dev/null
GENIEACS_NBI_ACCESS_LOG_FILE=/dev/null
GENIEACS_FS_ACCESS_LOG_FILE=/dev/null
